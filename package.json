{
  "name": "tessa",
  "version": "1.0.0",
  "description": "Figma plugin that analyzes design using Gemini API",
  "main": "code.js",
  "scripts": {
    "prebuild": "node scripts/inject-env.js",
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@google/generative-ai": "^0.15.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.99.0",
    "typescript": "^5.4.0"
  }
}
